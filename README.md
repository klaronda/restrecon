
  # nestrecon

  This is a code bundle for nestrecon. The original project is available at https://www.figma.com/design/cw6sJFvaGfnZ9ZbWEW0l3H/nestrecon.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  